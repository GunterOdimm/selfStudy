package study.java.code;

public class navertest02 {
	public static void main(String[] args) {
		String[] record = {"RECEIVE abcd@naver.com", "123"};
		String[] answer = {};
		String[] dratf= {};
		
		for(int i =0; i == record.length; i++) {
			int idx=record[i].indexOf("RECEIVE ");
			String emailADR = record[i].substring(idx+1);
			System.out.printf(emailADR);
			
		}
	}
}
